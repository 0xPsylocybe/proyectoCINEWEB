"""Configuracion de la app reservas."""

from django.apps import AppConfig


class ReservasConfig(AppConfig):
    """Configuracion de la app reservas."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "reservas"
